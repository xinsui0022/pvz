# v3.0.4：僵王冰火球碾碎地刺

在“我是僵尸”系列关卡（61–70，包括无尽）中，僵王的冰球、火球经过普通地刺所在格时，会按原生植物碾压流程将其压扁。地刺王仍保留原有免疫规则。

只替换 `UpdateBossFireball` 内 `0x535d43` 的调用：在上述模式将 `CrushPlantsInSquare` 的 `ignoreSpikeweed` 参数设为 false。共用碾压函数、命中行列、其他僵尸攻击和其他模式保持原有行为；冰火球继续前进及压脑子逻辑保留。

新增机器码测试复现旧版本跳过地刺，并覆盖冰火球两种状态、模式边界、普通植物、地刺王、相邻行列不被碾压及调用栈平衡。渲染和碾压音效未做游戏内实测。

2026-09-08：重建成功，完整自动化回归 65 项全部通过，包含补丁逐字节还原验证。

安装工具支持识别并备份 v3.0.3 后升级；补丁可逐字节恢复到最初的修改版 EXE。
