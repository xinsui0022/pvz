# 验证状态

## 自动化

执行：`python -m unittest discover -s tests -v`。

2026-09-08，v4.0.0 自动化回归 **79 项全部通过**（新增 14 项，原 65 项保留）。新增覆盖小丑 5%→20% 早爆及真实引信、90 像素爆脑范围／相邻行／魅惑／单次计分、舞王和蹦极的完整随机血量票号、原版伴舞抢跑和位移差异复现、1000 次行走与不同更新顺序、停止／减速／旧存档渐进归队、多队隔离／代际 ID／死亡成员、四方位召唤／失败路径以及寄存器和浮点状态。详见 [v4.0.0](v4.0.0.md)。

Unicorn 2.1.4 的裸 `FXSAVE; FLDPI; FXRSTOR` 最小实验确认它不会还原 FIP（最后一次 x87 指令地址），因此新增浮点测试仅排除 FIP 的四个字节，其余控制、状态、标记、FP/MMX/XMM 数据和 MXCSR 全部比较。实际补丁仍完整执行 FXSAVE/FXRSTOR。

V4 已安装到本地 EXE，安装结果与候选程序及差分包 SHA-256 一致：`7e48f0f3db6bd4aaa9258a58595dd43f0b8933916785871bf78a6219498d497a`。v3.0.4 备份为 `backups/PlantsVsZombies.v3.0.4.exe`，再次安装检查确认幂等；差分往返仍逐字节一致。本轮没有进行 V4 图形游戏实玩，视觉效果、复杂植物组合及无尽长局换关仍需实测。

2026-09-08，v3.0.2 清场奖励调整为 75 后，现有 59 项测试全部通过。已验证奖励叠加、植物豁免、单次结算及存档记录；与 v3.0.1 逐字节对比，仅清场奖励立即数和 PE 校验和发生变化，详见 [v3.0.2](v3.0.2.md)。已安装本地补丁，本轮未进行游戏内实测。

V3.0.1 自动化包含 59 个测试方法，2026-09-07 全部通过（方法内部另包含多种模式、血量、阶段、随机票号组合）：

- 原代码 `4539F1` 崩溃复现；正确指针、清理幂等、真正僵王关卡 prologue。
- 投篮 75 点、豌豆 20 点、混合攻击、过量伤害和零血量死亡；总阳光守恒。
- 寄存器、ESP、活跃 x87 值与浮点状态保存。
- 小丑／辣椒剩余阳光，非向日葵和其他模式。
- 海豚／潜水状态转换及动画调用参数；重复更新不会重新初始化；池边分支。
- 跳跳只在有脑子目标时失去跳杆。
- 辣椒和冰火球使用正确行；不重复计分；冰火球位置边界与 NaN。
- 随机票号穷举：35 个结果中僵王出现 3 次，所有 33 种类型仍可出现。
- 差分安装与还原逐字节相同，错误版本拒绝修改。
- 配套 XML／签名提取、安装、重复运行与还原；拒绝覆盖未知配置。
- V2 的奖励阶段、低血量权重、多僵王绘制、大嘴花矿工保护及交替蹦极价格。
- 失败 V3 的启动崩溃复现和修复、续接指令边界、冰道增加 1500 个计时单位。
- 窝瓜跳跃落点和压脑指针、脑子单次计分；雪人回头阶段和旧存档恢复。
- 僵王 500 血初始化、逐只下头记录、抬头前 50 个计时单位的无伤判定及浮点状态。
- 初始 V3 僵王 1 血当帧死亡复现；修复后执行完整更新路径，验证保留 1 血抬头、待机和再次下头，实际伤害后仍死亡，零伤害、存档、多僵王及模式边界正常。
- 暂停时的选卡限制、真实扣费、预约队列容量及存档、恢复后的原版生成流程、失焦不恢复。

引擎部分调用使用 ABI 测试桩，不能用这些结果声称动画或完整关卡已实测。

## 游戏内回归

用户已实测初始 V3：随机变身相关项目尚未测试，其他新增功能除僵王 1 血直接死亡外均正常。该问题已修复为 v3.0.1；本轮修复经过上述自动化回归，尚未在游戏内实测僵王动画。

2026-09-07 本轮 V3 已实际启动并截屏确认主菜单及无尽「我是僵尸」画面。实际预约测试：暂停无读报窗口，路障预约扣费 75、场上不生成僵尸；空格恢复后生成 1 只、预约清空、不重复扣费。测试前存档已备份到 `backups/userdata-before-v3-repair/`，存档不进 Git。细节见 [V3 说明](v3.0.0.md)。

建议依次复核：

1. **用户已确认通过**：从 Steam 启动，无 partner.xml 签名弹窗。
2. 四次投篮击中未受其他伤害的向日葵，最后一击植物消失，总共 200 阳光。
3. 豌豆／机枪与啃食混合攻击，阳光不漏发、不重复。
4. 小丑和辣椒炸掉部分受伤向日葵，掉落剩余阳光；辣椒同时消灭同一行脑子。
5. 海豚在陆地图跳过植物后游泳啃食，潜水僵尸潜游并上浮吃植物，两者都能吃脑子。
6. 跳跳在脑子前失去跳杆、落地后正常吃脑子。
7. 僵王冰球／火球消灭所在行脑子，其他行不受影响。
8. 多个僵王：分别死亡、仍有僵王时进入下一关、连续多关；确认无崩溃、残留球或重复分数。
9. 随机蹦极标记与落点；冰道多留 15 秒；奖励阳光可以手动拾取。
10. 窝瓜压脑完整动画；僵王无伤下头规则；雪人不回头；随机冰车和投篮不自爆。

原版潜水的“不能被普通直线攻击命中”仍由其状态机实现，并非对所有伤害无敌。水生动画在陆地背景上的遮挡仍需实际观察。


## V4.0.2 验证（2026-09-09）

完整自动化测试 99 项通过，覆盖黄油原生流程与旧存档断链恢复、僵王或向日葵的 20 秒完成延迟、F8 血量开关和绘制状态保护、雪橇队位置与虚拟滑行距离。已安装 EXE 与补丁清单散列一致。用户确认启动测试没有问题，并要求直接提交结束。未将其记录为全部新增玩法的逐项实测验收。

自动化启动曾因工作目录不正确出现 `properties\resources.xml` 找不到；已确认该文件存在于本地 `main.pak`，用户正常启动测试通过，无需替换资源包。
